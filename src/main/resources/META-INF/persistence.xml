<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">
    <persistence-unit name="miUnidadDePersistencia" transaction-type="RESOURCE_LOCAL">
        <!-- Configuración de la unidad de persistencia -->
        <!-- Proveedor de persistencia (por ejemplo, Hibernate) -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <!-- Configuración del origen de datos, clases de entidad, etc. -->
        <properties>
            <property name="javax.persistence.jdbc.url" value="jdbc:mariadb://localhost:3306/utn"/>
            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.password" value=""/>
            <!-- Otras propiedades de configuración -->
        </properties>
    </persistence-unit>
</persistence>

